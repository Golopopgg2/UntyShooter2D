using UnityEngine;

public class Projectile : MonoBehaviour
{
    // висит на префабе снаряда

    // 3. Удалять Projectile когда он вылетел за пределы сцены (см. метод юнити OnBecameInvisible)
    // 4. Также удалять + выводить сообщение, если снаряд попал по какой-то цели
}